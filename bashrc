# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=

# User specific aliases and functions

export SDMSHOME=/home/schedulix/schedulix-2.6.1
export CLASSPATH=$CLASSPATH:/usr/share/jflex-1.4.1/lib/JFlex.jar
export JAVAHOME=/usr/lib/jvm/java-1.7.0-openjdk
export SWTJAR=/usr/share/java/swt.jar
export JNAJAR=/usr/share/java/jna.jar

